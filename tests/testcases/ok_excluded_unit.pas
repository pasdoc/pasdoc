{ @exclude
  This is unit is not errorneous on itself.
  However, pasdoc should refuse (with proper error message)
  to generate documentation if only this unit is specified. }
unit error_excluded_unit;

interface

implementation

end.